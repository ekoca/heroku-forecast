// Dependencies
var appDependency = [
    'ngRoute',
    'ngResource',
    'angulartics',
    'angulartics.google.analytics',
    'angularSpinner',
] || appDependency;

// Module
var wheatherApp = angular.module('weatherApp', appDependency);